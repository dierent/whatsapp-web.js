# Normalize commit type to lowercase
node -e "const fs = require('fs'); const file = process.argv[1]; const msg = fs.readFileSync(file, 'utf8'); fs.writeFileSync(file, msg.replace(/^[A-Za-z]+/, (type) => type.toLowerCase()));" "$1"

npx --no-install commitlint --edit "$1"
